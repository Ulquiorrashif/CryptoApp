import { IsGrowDirective } from './is-grow.directive';

describe('IsGrowDirective', () => {
  it('should create an instance', () => {
    const directive = new IsGrowDirective();
    expect(directive).toBeTruthy();
  });
});
